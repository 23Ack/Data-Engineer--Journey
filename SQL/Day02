# 📅 Day 2 – SQL Aggregations

## 📖 Topics Covered
- Aggregate Functions
  - COUNT()
  - SUM()
  - AVG()
  - MIN()
  - MAX()
- GROUP BY
- HAVING
- WHERE vs HAVING
- CASE Expression
- Conditional Aggregation
- Subqueries
- Histogram Queries

---

## 📝 Problems Solved

### LeetCode
- ✅ 619. Biggest Single Number
- ✅ 1179. Reformat Department Table

### DataLemur
- ✅ Histogram of Tweets

---

## 💡 Key Learnings

- Used aggregate functions to summarize data.
- Learned how `GROUP BY` groups rows before applying aggregate functions.
- Understood the difference between `WHERE` and `HAVING`.
- Learned to use `CASE` for conditional aggregation.
- Understood how a subquery works as a temporary table.
- Built a histogram by performing aggregation on aggregated data.
- Learned the difference between `COUNT(*)` and `COUNT(column)`.
- Understood when to use a subquery instead of a correlated subquery.

---

## 🚀 SQL Concepts Mastered

- Aggregate Functions
- GROUP BY
- HAVING
- CASE
- Conditional Aggregation
- Subqueries
- Histograms

---

## 📌 Progress

- ✅ Day 1 Completed
- ✅ Day 2 Completed

**Next Goal:** SQL Joins (INNER, LEFT, RIGHT, FULL, SELF & CROSS JOIN)
